[profile.default]
src = "src"
out = "out"
libs = ["lib"]
remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "@chainlink/=lib/chainlink-brownie-contracts/",
]
rpc_endpoints = { base = "${BASE_RPC_URL}" }
etherscan_api_key = { base = "${BASESCAN_API_KEY}" }
